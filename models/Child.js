class Child {
    constructor(id, fullName, checkedIn, avatarSource) {
        this.id = id.toString();
        this.fullName = fullName;
        this.checkedIn = checkedIn;
        this.avatarSource = avatarSource;
    }
}

export default Child;